{{ form_start(formkiller) }}
    {{ form_widget(formkiller) }}
<button class="btn btn-primary" id="Update">{{ button_label|default('Save') }}</button>
{{ form_end(formkiller) }}
{#Création automatique du formulaire pour la modification d'un killer#}