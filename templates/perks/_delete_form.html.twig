<form method="post" action="{{ path('perks_delete', {'id': perks.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ perks.id) }}">
    <button id="Delete" class="btn" style="background-color: red; color: white">Delete</button>
</form>
{#Création du bouton de suppression d'une perk#}