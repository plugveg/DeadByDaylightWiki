{% extends 'base.html.twig' %}{#Permet d'importer la barre de navigation et tous les paramètres du fichier base.html.twig#}
{% block title %}Lecture du power {{ power.PowerName }}{% endblock %}{#Permet de donner un titre à son onglet#}
{% block body %}
    <style>
        /*Permet de définir le CSS de la page actuelle*/
        .center{
            display: block;
            margin-left: auto;
            margin-right: auto;
            width: 30%;
            height: 30%;
        }
        .centercard{
            display: block;
            margin-top: 2%;
            padding-top: 2%;
            margin-left: auto;
            margin-right: auto;
            width: 80%;
            text-align: center;
        }
    </style>
    {#Permet l'affichage du contenu de la page#}
    <div class="card text-white bg-dark mb-3 centercard">
        <img class="card-img-top center" src="{{ power.PowerImage }}" alt="Card image cap" >
        <div class="card-body">
            <h1 class="card-title">{{ power.PowerName }}</h1>
        </div>
        <ul class="list-group list-group-flush">
            <li class="list-group-item"><h5>Power Description : </h5><br>{{ power.PowerDescription | nl2br }}</li>
            <li class="list-group-item"><h5>Power Explanation : </h5><br>{{ power.PowerExplanation | nl2br }}</li>
        </ul>
        <div class="card-body">
            <a class="card-link" href="{{ path('killer_read', {'id': power.id}) }}">Go to the killer</a>
            {#Redirection vers le formulaire d'edit si on est ADMIN#}
            {% if is_granted('ROLE_ADMIN') %}
                <a class="card-link" href="{{ path('powers_edit', {'id': power.id}) }}">Edit the power</a>
            {% endif %}
        </div>
    </div>
{% endblock %}