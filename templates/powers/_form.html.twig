{{ form_start(formpower) }}
    {{ form_widget(formpower) }}
<button class="btn btn-primary" id="Update">{{ button_label|default('Save') }}</button>
{{ form_end(formpower) }}
{#Création automatique du formulaire pour la modification d'un power#}