{{ form_start(formuser) }}
    {{ form_widget(formuser) }}
<button class="btn btn-primary" id="Update">{{ button_label|default('Save') }}</button>
{{ form_end(formuser) }}
{#Création automatique du formulaire pour la modification d'un user#}