{{ form_start(formperkkiller) }}
    {{ form_widget(formperkkiller) }}
<button class="btn btn-primary" id="Update">{{ button_label|default('Save') }}</button>
{{ form_end(formperkkiller) }}
{#Création automatique du formulaire pour la modification d'une perk killer#}