{% extends 'base.html.twig' %}
{% block body %}
    <style>
        .center{
            display: block;
            margin-left: auto;
            margin-right: auto;
            width: 30%;
            height: 30%;
        }
        .centercard{
            display: block;
            margin-top: 2%;
            padding-top: 2%;
            margin-left: auto;
            margin-right: auto;
            width: 80%;
            text-align: center;
        }
    </style>

    <div class="card text-white bg-dark mb-3 centercard">
        <img class="card-img-top center" src="{{ map.MapImage }}" alt="Card image cap" >
        <div class="card-body">
            <h1 class="card-title">{{ map.MapName }}</h1>
        </div>
        <ul class="list-group list-group-flush">
            <li class="list-group-item"><h5>Map Description : </h5><br>{{ map.MapDescription | nl2br }}</li>
            <li class="list-group-item"><h5>Map Layout : </h5><br><img src="{{ map.MapLayout }}" alt="layout map"></li>
        </ul>
        <div class="card-body">
            {% if is_granted('ROLE_ADMIN') %}
                <a class="card-link" href="{{ path('maps_edit', {'id': map.id}) }}">Edit the maps</a>
            {% endif %}
        </div>
    </div>
{% endblock %}