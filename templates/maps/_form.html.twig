{{ form_start(formmap) }}
    {{ form_widget(formmap) }}
<button class="btn btn-primary" id="Update">{{ button_label|default('Save') }}</button>
{{ form_end(formmap) }}
{#Création automatique du formulaire pour la modification d'une map#}