<form method="post" action="{{ path('maps_delete', {'id': maps.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ maps.id) }}">
    <button id="Delete" class="btn" style="background-color: red; color: white">Delete</button>
</form>
